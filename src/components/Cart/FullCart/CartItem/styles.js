import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    cardMed: {
        height: 0,
        paddingTop: '56.25%', // 16:
    }
}))

export default useStyles;